# End-to-End Audit Tracker

Canonical tracker for the June 2026 app audit. Status values: `pass`, `fixed`, `blocked`.

Manual DB QA used a disposable Neon branch, `br-noisy-flower-ane92wx9`, created from project `delicate-mud-37709227`. The app ran locally at `http://localhost:3000` with that branch connection string, so setup, scorecard, publish, admin, and dry-run mutations did not target the default branch. Preview deployment `dpl_6X6J3A7DQU754FT319kS2GxkZuwF` reached `READY` at `https://thetwo-2blfktf6p-jcb79107s-projects.vercel.app` and was smoke-tested read-only. Sentry CLI access was verified for org `jason-baer`, project `javascript-nextjs`; latest production release `6ad8494567135d73ff9343d713c75cf79b04b10a` had zero new groups, no issues attached to the release, and no production error aggregates in the last 7 days. Microsoft Clarity Data Export API was queried for the last 72 hours: 46 sessions, 42 distinct users, 0 bot sessions, 0 script errors, 0 error clicks, 0 rage clicks, and 0 excessive-scroll events. Browser screenshot capture through CDP was intermittently unreliable, so rendered evidence is primarily URL/title/DOM/console based.

| Feature area | User story | Expected behavior based on the code | Relevant files/routes/components | Test steps | Current status | Bugs or UX issues found | Fix notes |
|---|---|---|---|---|---|---|---|
| Public home | As a visitor, I can open `/` and land on the latest tournament home. | Loads latest tournament home with feed/nav; local fallback renders when no DB state exists. | `app/page.tsx`, `src/components/tournament-home-view.tsx`, `src/lib/server/public-tournament.ts` | Unit route tests; browser `/`; dry-run public endpoint check. | pass | Initial audit only had fallback because local `.env.local` DB auth failed. | Branch-backed home rendered live tournament feed and nav. |
| Public tournament home | As a visitor with a tournament slug, I can view tournament activity and key links. | `/tournament/[slug]` renders feed, rules judge, and public nav for a valid slug. | `app/tournament/[slug]/page.tsx`, `src/components/activity-feed.tsx`, `src/components/public-nav.tsx` | Browser `/tournament/the-two-man-2026`; dry-run endpoint check. | pass | None remaining. | Final feed showed completed result link after publish. |
| Public navigation | As a visitor, I can move between home, standings, bracket, rules, Instagram, and scorecard links. | Sticky/mobile nav uses `ROUTES`; season live badge appears when active. | `src/components/public-nav.tsx`, `src/lib/api/routes.ts` | Browser checked nav links on branch-backed pages. | fixed | Next dev warned the logo was LCP without priority. | Added `priority` to the nav logo. |
| Public feed | As a visitor, I can see scheduled, ready, final, playoff, semifinal, and championship feed events. | Feed is computed from matches/bracket state; result events link to public match pages. | `src/lib/server/public-tournament.ts`, `src/components/activity-feed.tsx` | Unit formatting tests; branch publish; dry-run. | pass | None remaining. | Final event `Baer & Cadden def. Agins & Deutsch 17-1` linked to the result page. |
| Standings pods tab | As a competitor, I can view pod standings and current leaders. | Sorts by record, hole points, holes won, then net better-ball. | `app/tournament/[slug]/standings/page.tsx`, `src/components/standings-table.tsx`, `src/lib/server/standings.ts` | Unit standings tests; browser before/after publish; dry-run. | pass | None remaining. | Pod 1 updated after branch publish. |
| Standings playoff tab | As a competitor, I can view projected or locked playoff field and wild-card bubble. | Shows projected field after pod results and locked field after pod play completes. | `app/tournament/[slug]/standings/page.tsx`, `src/lib/server/qualification.ts` | Unit qualification tests; browser `?tab=playoff`; dry-run. | pass | None remaining. | Browser showed projected field after one result; dry-run locked 8/8 seeds. |
| Standings all-teams tab | As a competitor, I can scan and sort every team. | `?tab=teams` shows all rows and sortable columns/markers. | `app/tournament/[slug]/standings/page.tsx`, `src/components/all-teams-table.tsx` | Browser `?tab=teams`; table buttons visible. | pass | None remaining. | All 18 teams rendered with sort controls. |
| Bracket before lock | As a visitor before playoffs, I can see the seed path. | Shows quarterfinal seed placeholders while pod play is incomplete. | `app/tournament/[slug]/bracket/page.tsx` | Browser bracket before dry-run. | pass | None remaining. | Placeholder quarterfinal path rendered. |
| Bracket after lock | As a visitor after bracket creation, I can inspect rounds and winners. | Decorated bracket renders QF/SF/Championship rounds, winners, and match links. | `app/tournament/[slug]/bracket/page.tsx`, `src/components/bracket-view.tsx`, `src/lib/server/bracket.ts` | Unit bracket tests; dry-run completed playoff bracket. | pass | One transient Next dev HMR error appeared during heavy dry-run mutation. | Production build will be the source of truth; dev server restart planned before final smoke. |
| Rules page | As a player, I can read rules, FAQ, full-doc link, and rules judge link. | Valid tournament renders static rules content and external links. | `app/tournament/[slug]/rules/page.tsx`, `src/lib/content/rules-judge.ts` | Browser rules page; dry-run endpoint check. | pass | None remaining. | Rules content rendered with full-doc and rules-judge links. |
| Public match shell | As a visitor, I can open a match page without private data. | Non-final pages withhold full card; final pages show official card. | `app/tournament/[slug]/matches/[matchId]/page.tsx` | Dry-run public match endpoint; branch final match browser check. | pass | Non-final shell was not separately preserved after dry-run completed all matches. | Public final route verified; route/build tests cover shell. |
| Public match scorecard | As a visitor, I can view a final match scorecard. | Final/forfeit matches show result, course, summaries, setup, and full card. | `app/tournament/[slug]/matches/[matchId]/page.tsx`, `src/components/public-match-scorecard.tsx` | Branch publish; browser final match page. | pass | None remaining. | Official scorecard rendered for match `a423992b1db24c0cb841d`. |
| Invite token redirect | As a player using an invite link, I am redirected to the private match. | `/invite/[token]` marks `openedAt` and redirects; invalid token 404s. | `app/invite/[token]/page.tsx`, `src/lib/server/invitations.ts` | Unit invite tests; code inspection of admin schedule action. | pass, blocked external send | Existing pod-link generation uses private match links, not `MatchInvitation` rows. | `npm test` covers redirect; Gmail/send path intentionally not submitted. |
| Private match routing | As a player, `/match/[token]` sends me to setup or scorecard. | Token routes to setup until complete, then scorecard; final cards are read-only. | `app/match/[token]/*`, `src/lib/server/matches.ts` | Unit tests; browser private final link. | pass | None remaining. | Private final link had zero inputs and only front/back toggles. |
| Course search | As a scorekeeper, I can search courses by name/state. | API returns serialized stored/provider courses and tees. | `app/api/courses/search/route.ts`, `src/lib/server/course-catalog.ts` | Browser searched Aldeen; curl measured route. | fixed | Stored Aldeen lookup waited on external provider path and took about 15.1s. | Added stored full-scorecard fast path; Aldeen returned in about 0.82s with 12 tees and 18-hole data. |
| Manual course | As a scorekeeper, I can enter a manual course when lookup fails. | Manual setup validates course, tee, rating/slope, pars, and stroke indexes. | `src/components/private-match-workspace.tsx`, scorecard API route | Branch API setup with throwaway match and 18-hole manual tee. | pass | None remaining. | Manual setup created `Manual Audit Course`, `Audit tees`, 18 holes, four players, and scorecard template. |
| Setup handicaps and tees | As a scorekeeper, I can set all four Handicap Indexes and tees. | Setup requires course, tee data, four valid indexes, tee selection, and confirmation. | `src/components/private-match-workspace.tsx`, scorecard API route | Browser admin setup with Aldeen and indexes 8-11. | pass | None remaining. | Setup saved and redirected to scorecard. |
| Setup confirmation gate | As a scorekeeper, I must confirm day-of values before scorecard generation. | Generate button stays disabled until blockers clear and checkbox is checked. | `src/components/private-match-workspace.tsx` | Browser before/after course/index/confirmation. | pass | None remaining. | Button disabled with missing indexes; enabled after confirmation. |
| Score entry | As a scorekeeper, I can enter gross scores and see summaries update. | Score inputs accept 1-20 and recompute net/better-ball summaries. | `src/components/private-match-workspace.tsx`, `src/components/scorecard-table.tsx` | Browser entered hole 1 scores; API validation tests. | pass | None remaining. | `1/18 holes filled` and team-net summary updated. |
| Local draft restore | As a scorekeeper, local score drafts are protected on the same device. | LocalStorage restore/clear guards in-progress cards. | `src/components/private-match-workspace.tsx` | Browser score entry plus reload-adjacent checks. | pass | None remaining observed. | Autosave/local draft labels appeared during score entry; full same-device restore remains lower risk after publish clear. |
| Server draft autosave | As a scorekeeper, score edits are saved after editing. | Debounced `saveDraft` sets `IN_PROGRESS` and persists scores. | `src/components/private-match-workspace.tsx`, scorecard API route | Browser score entry; API GET/POST; branch DB-backed save. | pass | None remaining. | Draft scores were visible through the API before publish. |
| Publish scorecard | As a scorekeeper, I can publish a complete card. | API validates scores, stores official snapshot, finalizes match, creates feed event, syncs bracket. | `app/api/matches/[token]/scorecard/route.ts`, `src/lib/server/scorecard-validation.ts` | Route regression tests; branch publish; dry-run. | fixed | Route previously bypassed strict duplicate/unknown/missing-row helper validation. | Wired helper into route; branch publish finalized 18 holes and updated feed/standings. |
| Playoff tiebreak selection | As a playoff scorekeeper, I must select a winner if hole points tie. | Non-pod tied publishes require `playoffWinnerTeamId`. | Scorecard API, scoring engine, dry-run script | Unit tests; dry-run playoff tied path. | pass | None remaining. | Dry-run exercised playoff tiebreak publication. |
| Published read-only card | As a player, I cannot alter a final card from a normal private link. | Final private page renders public scorecard view with no editable inputs. | `app/match/[token]/scorecard/page.tsx` | Browser private final link after publish. | pass | None remaining. | Private final had `inputCount: 0`. |
| Admin authentication | As commissioner, I can sign in with the configured password. | Config warning when env missing; otherwise server compares full `ADMIN_PASSWORD` and sets signed cookie. | `app/admin/page.tsx`, `app/admin/actions.ts`, `src/lib/server/admin-auth.ts`, `src/lib/server/admin-validation.ts` | Browser login with local configured password. | fixed | UI and Zod parser incorrectly required a 4-digit numeric passcode while env used a full password string. | Removed numeric/max-length constraints, changed copy to password, added validation test. |
| Admin dashboard overview | As commissioner, I can view live/final/needs counts, production checks, rescue list, and activity. | Authenticated admin aggregates latest tournament data. | `app/admin/page.tsx`, `src/lib/server/admin.ts` | Browser after login. | pass | None remaining. | Dashboard rendered all major sections. |
| Admin email manager | As commissioner, I can generate/copy/send private match invites. | Generates missing pod private links, builds compose URLs, tracks sent state in localStorage. | `src/components/admin-match-ops-list.tsx`, `src/components/email-invite-button.tsx` | Browser email manager and generate action. | fixed | `useSyncExternalStore` snapshot returned fresh `{}` when localStorage was empty, causing React infinite-loop warning. | Cached empty snapshot; generate action returned success. External Gmail send not submitted. |
| Admin scorecard manager | As commissioner, I can search/filter/open/reset/edit cards. | Filters by state; links into setup/scorecard; reset is server action. | `src/components/admin-match-ops-list.tsx`, admin match routes | Browser scorecards section; opened setup/scorecard. | pass | None remaining for open/filter surface. | Destructive reset not clicked beyond dry-run branch reset logic. |
| Admin final-card override | As commissioner, I can correct a posted card. | Published admin cards enter override mode; drafts set `REOPENED`; republish snapshots result. | Admin scorecard route, scorecard API, dry-run | Unit route tests; dry-run reopen/override path. | fixed | Fresh READY admin cards displayed “posted card” and “Submit override” copy. | Override mode now only applies when admin scorecard is published; dry-run validated actual override path. |
| Admin reopen/reset/forfeit | As commissioner, I can reopen, reset, or mark forfeit with audit trail. | Server actions require admin session, update scores/snapshots/audit, and sync bracket. | `app/admin/actions.ts`, `src/lib/server/bracket-sync.ts` | Unit tests; dry-run reopen rollback. | pass | None remaining. | Dry-run reopened QF2 after advancement, cleared stale downstream state, and republished. |
| Admin roster/team/pod/import | As commissioner, I can maintain tournament field data. | Validation/import parsers enforce required fields, team uniqueness, pod slots, and CSV checks. | `app/admin/actions.ts`, `src/lib/server/admin-validation.ts`, `src/lib/server/admin-import.ts` | Unit tests. | pass | No visible UI for several low-level actions in current admin surface. | Validation/import tests pass; no manual form UI for every action. |
| Admin bracket settings and sync | As commissioner, I can create/update bracket shell and sync advancement. | Bracket actions create rounds and sync winners/seeds. | `app/admin/actions.ts`, `src/lib/server/bracket-sync.ts` | Unit tests; dry-run full bracket. | pass | None remaining. | Dry-run completed 18 pod matches and 7 playoff matches. |
| Admin graphics UI/API | As commissioner, I can preview/download Instagram recap graphics. | Graphics list final recaps; API returns PNG behind admin auth. | `src/components/admin-instagram-graphic-generator.tsx`, `app/api/admin/graphics/[id]/route.ts` | Unit export tests; admin graphics section after auth. | pass | Browser PNG download not saved during audit. | Export tests pass; section rendered after admin auth. |
| Admin bootstrap API | As an authenticated preview bootstrapper, I can retrieve dashboard summary. | POST requires admin auth and returns tournament summary or errors. | `app/api/admin/tournament/bootstrap/route.ts` | Unit API tests. | pass | None remaining. | Tests pass. |
| Sentry verification API | As commissioner, I can trigger a controlled Sentry error after deploy. | GET requires admin auth, captures/flushes Sentry, returns event id. | `app/api/admin/sentry-test/route.ts`, Sentry config | Code inspection; Sentry CLI project/release/issues/explore checks; stale issue cleanup. | pass | Historical stale Sentry issues were resolved against release `ed187c702cb049bcd5669b516e1dc0f05dc80b30` so future regressions reopen cleanly. | Did not intentionally trigger the test-error endpoint; verified live Sentry ingest via releases, deploys, issue search, and zero current production error aggregates. |
| Public standings API | As an integration client, I can fetch standings JSON. | Returns pod standings, wild cards, projections, and bubble. | `app/api/public/tournament/[slug]/standings/route.ts` | Unit tests; dry-run endpoint check. | pass | None remaining. | Dry-run checked endpoint 200. |
| Public bracket API | As an integration client, I can fetch bracket JSON. | Returns bracket seeds/rounds or 404. | `app/api/public/tournament/[slug]/bracket/route.ts` | Dry-run endpoint check. | pass | None remaining. | Dry-run checked endpoint 200. |
| Private scorecard API read | As the client app, I can hydrate a private scorecard by token. | GET returns `PrivateMatchView` for valid token. | `app/api/matches/[token]/scorecard/route.ts` | Branch GET before publish; route tests. | pass | None remaining. | API returned 4 players and 18-hole template. |
| Scoring engine | As the app, scoring is deterministic. | Applies allowances, strokes, points, wins/ties/forfeits/snapshots. | `src/lib/scoring/engine.ts`, `src/lib/scoring/types.ts` | Unit tests; dry-run. | pass | None remaining. | Tests and dry-run passed. |
| Official snapshots | As the app, final results remain stable. | Final/forfeit store versioned official snapshots and public/admin reads prefer them. | `src/lib/server/official-result-snapshot.ts` | Unit tests; branch publish; dry-run override. | pass | None remaining. | Final public scorecard rendered from official result. |
| Public scorecard corrections | As an operator, legacy scorecard corrections can be applied safely. | Targeted corrections adjust named scorecards and recompute summaries. | `src/lib/server/public-scorecard-corrections.ts`, tests | Unit tests; typecheck. | fixed | Empty test fixtures inferred as `never[]`, breaking `tsc --noEmit`. | Typed fixtures as `TestHoleScore[]`. |
| Course catalog providers | As a scorekeeper, lookup uses stored catalog, curated fallbacks, and providers. | Search dedupes/merges/persists lookup results. | `src/lib/server/course-catalog.ts`, provider files | Unit fallbacks; branch lookup; measured fast path. | fixed | Stored full-scorecard hits were delayed by provider lookups. | Stored full-scorecard matches now return before provider calls. |
| GHIN integration boundary | As a future operator, handicap sync is represented but not active. | Player model stores GHIN/sync status; manual index remains workflow. | `prisma/schema.prisma`, providers | Code inspection. | pass | GHIN sync intentionally not operational. | No fix needed. |
| Database model integrity | As the app, tournament/course/match/score/audit/invite data persists correctly. | Prisma schema defines required relations, uniqueness, and cascades. | `prisma/schema.prisma` | Static inspection; branch mutations; dry-run. | pass | None remaining. | Disposable branch schema/data worked for setup, publish, bracket, and dry-run. |
| Seed data | As a developer, I can seed demo state. | `prisma/seed.ts` deletes and recreates demo tournament data. | `prisma/seed.ts` | `npm run prisma:seed` on separate seed-audit branch. | pass | Seed is destructive, so it was not run on the main QA branch. | Seed branch `br-noisy-rain-anhyb8y6` produced 1 tournament, 18 teams, 36 players, 25 matches, 100 invitations, and 72 hole scores. |
| Operator scripts | As an operator, I can dry-run and validate tournament progression. | Scripts cover dry-run, reset, staging, imports, snapshots, rollout assets, and observability checks. | `scripts/*`, `package.json` scripts | `npm run dry-run:tournament -- the-two-man-2026` on branch; `npm run observability:check -- --skip-clarity`. | pass | Broader import/export scripts may require provider/network credentials; Clarity token is optional/local-only. | Dry-run completed and wrote `docs/dry-run-report.md`; `observability:check` passed Sentry release/current production checks. |
| Deployment/config | As a deployer, build/runtime config is sane. | Next/Vercel config build with Prisma; deploy script enforces Git sync. | `next.config.ts`, `vercel.json`, deploy script | Lint/type/test/build; Vercel preview deploy; read-only smoke for `/`, `/tournament/the-two-man-2026`, and `/admin`. | pass | None remaining locally. | Preview `dpl_6X6J3A7DQU754FT319kS2GxkZuwF` reached `READY`; sampled routes returned HTTP 200 and expected app markers. |
| Error/loading/metadata | As a user, I get branded loading/not-found/error/metadata surfaces. | App defines layout metadata, robots/sitemap, loading shells, not-found, global error. | `app/layout.tsx`, `app/not-found.tsx`, metadata routes | Browser invalid route; curl metadata; build. | pass | None remaining. | Branded 404, robots, sitemap verified. |
| Observability/privacy | As operator, telemetry captures errors without leaking private tokens. | Sentry gated by DSN; scrubber redacts match URLs/tokens; Clarity gated by env. | `instrumentation*.ts`, `src/lib/sentry-scrub.ts`, `app/layout.tsx`, `scripts/observability_check.mjs` | Static inspection; env check; build; Sentry CLI production release/issues/explore checks; Clarity Data Export API last-72-hour URL/device/browser checks; `npm run observability:check -- --skip-clarity`. | pass | Clarity showed 6 dead clicks across 4 sessions and 18 quickbacks, mainly around `/`, standings, standings tabs, and bracket; no script errors, error clicks, rage clicks, or excessive scroll. | Added `npm run observability:check`; stale Sentry issues resolved; current release `ed187c702cb049bcd5669b516e1dc0f05dc80b30` has zero new groups, zero attached issues, zero unresolved production issues in 24h, and zero production error aggregate rows. Clarity export remains a low-volume UX watch item without replay click-target detail. |
