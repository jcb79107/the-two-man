# End-to-End Audit Tracker

Canonical tracker for the June 2026 app audit. Status values: `untested`, `pass`, `fail`, `fixed`, `retest needed`, `blocked`.

Manual browser QA environment: `npm run dev` on `http://localhost:3000` starts successfully, but the configured `.env.local` Postgres/Neon credentials fail authentication and no local Postgres/Docker/psql is available in this workspace. Database-backed browser flows are therefore marked blocked where they require live tournament data or safe mutations. Read-only fallback/admin-login states were still checked with the in-app Browser. Browser screenshot capture also timed out through CDP, so rendered evidence is DOM/URL/title/console based.

| Feature area | User story | Expected behavior based on the code | Relevant files/routes/components | Test steps | Current status | Bugs or UX issues found | Fix notes |
|---|---|---|---|---|---|---|---|
| Public home | As a visitor, I can open `/` and land on the latest tournament home. | Looks up latest tournament slug, loads public tournament state, shows feed and live-season nav. If no data exists, shows local preview fallback; preview demo fallback is only enabled when `VERCEL_ENV=preview` and `FAIRWAY_ENABLE_PREVIEW_DEMO=1`. | `app/page.tsx`, `src/components/tournament-home-view.tsx`, `src/lib/server/public-tournament.ts` | Unit route tests; browser load `/`. | pass | Live tournament state blocked by DB auth, but local fallback rendered correctly at `/` with title `The Two Man`, no console errors, and no error-overlay text. | `npm test` passed `home-routing` and `tournament-home-routing`; Browser DOM/console checked fallback. |
| Public tournament home | As a visitor with a tournament slug, I can view tournament activity and key links. | `/tournament/[slug]` renders the same home view for valid slugs, 404s invalid slugs, and can use preview fallback in preview env. | `app/tournament/[slug]/page.tsx`, `src/components/activity-feed.tsx`, `src/components/public-nav.tsx` | Unit route tests; browser load valid and invalid slug. | pass (automated), blocked manual | Browser valid-slug check requires working tournament DB state. | `npm test` passed route tests; manual DB-backed render blocked by invalid DB credentials. |
| Public navigation | As a visitor, I can move between home, standings, bracket, rules, Instagram, and scorecard links. | Sticky/mobile public nav links use `ROUTES`; season-live state affects visible nav affordances. | `src/components/public-nav.tsx`, `src/lib/api/routes.ts` | Browser click nav links desktop and mobile. | blocked | Public nav is only rendered on DB-backed tournament pages in this environment. | Manual click testing blocked by invalid DB credentials; route constants inspected and build passed. |
| Public feed | As a visitor, I can see scheduled, in-progress, completed, playoff, semifinal, and championship feed events. | Feed is computed from matches and bracket state; match events link to public match pages; bracket shortcut events link to bracket rounds. | `src/lib/server/public-tournament.ts`, `src/components/activity-feed.tsx`, `src/lib/server/public-feed-formatting.ts` | Unit formatting tests; browser inspect feed links and labels. | pass (automated), blocked manual | Browser feed requires DB-backed tournament state. | `npm test` passed public feed formatting; manual render blocked by invalid DB credentials. |
| Standings pods tab | As a competitor, I can view pod standings and current leaders. | `/tournament/[slug]/standings` defaults to pods; before results it shows opening-day empty standings; after results it sorts by record, hole points, holes won, then lowest net better-ball. | `app/tournament/[slug]/standings/page.tsx`, `src/components/standings-table.tsx`, `src/lib/server/standings.ts` | Unit standings tests; browser load pods tab. | pass (automated), blocked manual | Browser standings require DB-backed tournament state. | `npm test` passed standings tests; manual render blocked by invalid DB credentials. |
| Standings playoff tab | As a competitor, I can view projected or locked playoff field and wild-card bubble. | `?tab=playoff` shows projected field after any pod result and locked field when all pod play is complete and computed seeds match the field size. | `app/tournament/[slug]/standings/page.tsx`, `src/lib/server/qualification.ts` | Unit qualification tests; browser load playoff tab before/after seeded data. | pass (automated), blocked manual | Browser playoff tab requires DB-backed tournament state. | `npm test` passed qualification tests; manual render blocked by invalid DB credentials. |
| Standings all-teams tab | As a competitor, I can sort and scan every team. | `?tab=teams` shows all team rows with markers for projected/clinched/eliminated status and sortable table UI. | `app/tournament/[slug]/standings/page.tsx`, `src/components/all-teams-table.tsx` | Browser sort columns and check marker labels. | blocked | Browser all-teams tab requires DB-backed tournament state. | Manual render/sort blocked by invalid DB credentials; build/typecheck passed component code. |
| Bracket page before lock | As a visitor before playoffs, I can see the seed path and projected teams. | If no bracket rounds exist, page shows quarterfinal seed pairs and projected teams if available. | `app/tournament/[slug]/bracket/page.tsx`, `src/lib/server/public-tournament.ts` | Browser load bracket with incomplete field. | blocked | Browser bracket state requires DB-backed tournament data. | Manual render blocked by invalid DB credentials; build passed route. |
| Bracket page after lock | As a visitor after bracket creation, I can navigate rounds and open match pages. | Renders decorated bracket rounds, match shell labels, winners, and round navigation from `BracketView`. | `app/tournament/[slug]/bracket/page.tsx`, `src/components/bracket-view.tsx`, `src/lib/server/bracket.ts` | Unit bracket tests; browser click round tabs/arrows and match cards. | pass (automated), blocked manual | Browser bracket interaction requires DB-backed tournament data. | `npm test` passed bracket tests; manual render blocked by invalid DB credentials. |
| Rules page | As a player, I can read tournament rules, full-doc link, match procedure, handicap rules, FAQ, and live rules help. | `/tournament/[slug]/rules` requires valid tournament, shows static rules content, Google Doc, and rules-judge external link. | `app/tournament/[slug]/rules/page.tsx`, `src/lib/content/rules-judge.ts` | Browser load rules, expand FAQ, open external links without submitting data. | blocked | Route requires valid tournament state before rendering static rules. | Manual render blocked by invalid DB credentials; build passed route. |
| Public match shell | As a visitor, I can open a non-final match and see status without private data. | Public match page shows status shell and withholds full scorecard until final/forfeit scorecard snapshot exists. | `app/tournament/[slug]/matches/[matchId]/page.tsx`, `src/lib/server/public-tournament.ts` | Browser open scheduled/ready/in-progress public match. | blocked | Public match state requires DB-backed tournament and match records. | Manual render blocked by invalid DB credentials; build passed route. |
| Public match scorecard | As a visitor, I can view a final match scorecard. | Final/forfeit matches render public scorecard, result label, course/location/date, team summaries, hole story, card setup, and expandable full card. | `app/tournament/[slug]/matches/[matchId]/page.tsx`, `src/components/public-match-scorecard.tsx`, `src/lib/server/official-result-snapshot.ts` | Browser open final seeded match; expand full card. | pass (automated), blocked manual | Browser final-card render requires DB-backed tournament and match records. | `npm test` passed snapshot and correction tests; manual render blocked by invalid DB credentials. |
| Invite token redirect | As a player using an invite link, I am redirected to the private match. | `/invite/[token]` marks `openedAt` on first open and redirects to `/match/[privateToken]`; invalid token 404s. | `app/invite/[token]/page.tsx`, `src/lib/server/invitations.ts` | Unit invite tests; browser open valid and invalid invite. | pass (automated), blocked manual | Browser invite redirect requires DB-backed invitation records and mutates `openedAt`. | `npm test` passed invite routing; manual blocked by invalid DB credentials and no safe local DB. |
| Private match entry routing | As a player, `/match/[token]` sends me to setup or scorecard. | DB-backed token routes to setup until four selections exist, then scorecard; demo fallback exists only for mock tokens; invalid token 404s. | `app/match/[token]/page.tsx`, `src/lib/server/matches.ts` | Unit private routing tests; browser open seeded token states. | pass (automated), blocked manual | Browser DB-backed token states require working DB credentials. | `npm test` passed private routing; manual render blocked by invalid DB credentials. |
| Match setup course search | As a scorekeeper, I can search courses by name/state. | Client calls `/api/courses/search`; route requires name, defaults state to IL, queries stored catalog plus providers/fallbacks, returns serialized courses and tees. | `src/components/private-match-workspace.tsx`, `app/api/courses/search/route.ts`, `src/lib/server/course-catalog.ts`, provider files | Browser search empty query and valid course; API smoke for missing name. | pass (automated/static), blocked manual | Manual course search requires DB/provider access; external providers may also require keys/network. | Course fallback unit tests pass; build/typecheck route passed; browser/API runtime blocked by invalid DB credentials. |
| Match setup manual course | As a scorekeeper, I can enter a manual course when lookup fails or tee data is missing. | Manual setup validates name, tee, rating 50-85, slope 55-155, 18 par values, and unique stroke indexes 1-18, then persists a match-specific course/tee. | `src/components/private-match-workspace.tsx`, `app/api/matches/[token]/scorecard/route.ts` | Browser enter invalid and valid manual course; POST API validation. | blocked | Manual setup POST mutates course/match data and requires safe DB state. | Blocked by invalid DB credentials and no local DB; route validation inspected/build passed. |
| Match setup player handicaps and tees | As a scorekeeper, I can set Handicap Index and tees for all four players. | Setup requires course, tee data, four distinct roster players, each Handicap Index 0-54, and selected tee; then creates match player snapshots and 18-hole score template. | `src/components/private-match-workspace.tsx`, `app/api/matches/[token]/scorecard/route.ts`, `src/lib/scoring/engine.ts` | Browser setup flow; API invalid player/handicap/tee cases. | pass (automated), blocked manual | Browser/API setup mutates match data and requires safe DB state. | `npm test` passed scoring engine tests; manual/API runtime blocked by invalid DB credentials and no local DB. |
| Setup confirmation gate | As a player, I must confirm day-of indexes/course/tees before generating scorecard. | UI disables generate button until blockers clear and confirmation checkbox is checked. | `src/components/private-match-workspace.tsx` | Browser verify disabled/enabled states and blocker copy. | blocked | Requires rendering a DB-backed private match setup workspace. | Manual UI test blocked by invalid DB credentials; build/typecheck passed. |
| Score entry | As a scorekeeper, I can enter gross scores on front/back scorecard. | Inputs accept numeric text only, max two digits and <=20; scorecard computes gross/net/best-ball summaries live. | `src/components/private-match-workspace.tsx`, `src/components/scorecard-table.tsx`, `src/components/match-scorecard-summary.tsx` | Browser enter valid/invalid scores, switch front/back, verify summaries. | pass (automated API), blocked manual | Browser score entry requires DB-backed private match workspace. | Strict API score validation now covered by `tests/private-scorecard-api-validation.test.ts`; manual UI blocked by invalid DB credentials. |
| Local draft restore | As a scorekeeper, my in-progress card is protected on the same device. | Client stores setup/scores in localStorage, restores once per load, clears on publish, and reports local/server draft status. | `src/components/private-match-workspace.tsx` | Browser enter scores, reload, verify restored banner and persisted values. | blocked | Requires rendering a DB-backed private match workspace. | Manual UI test blocked by invalid DB credentials. |
| Server draft autosave | As a scorekeeper, scores are saved after editing. | Score edits debounce for 1200ms and POST `saveDraft`; status becomes `IN_PROGRESS`; failure falls back to local draft. | `src/components/private-match-workspace.tsx`, `app/api/matches/[token]/scorecard/route.ts` | Browser edit score and reload from server; inspect API response. | pass (automated validation), blocked manual | Save-draft runtime mutates match data and requires safe DB state. | Malformed save-draft payloads now route-tested; browser/server autosave blocked by invalid DB credentials and no local DB. |
| Publish scorecard | As a scorekeeper, I can publish only a complete endorsed card. | UI requires all holes filled and all player endorsements; API saves scores, computes official result snapshot, finalizes match, creates public feed event, and syncs bracket. | `src/components/private-match-workspace.tsx`, `app/api/matches/[token]/scorecard/route.ts`, `src/lib/server/bracket-sync.ts` | Browser publish complete card; API malformed payload tests. | fixed, blocked manual | API route did not use existing strict scorecard-validation helper, so duplicate/unknown/missing row checks tested in `src/lib/server/scorecard-validation.ts` were not enforced by the actual route. Browser publish mutates match data and requires safe DB state. | Wired the route to `validateSubmittedScoreRows`/`buildPublishedHoleScores` and added `tests/private-scorecard-api-validation.test.ts`; full `npm test` passes 21 files / 91 tests. Manual browser publish blocked by invalid DB credentials and no local DB. |
| Playoff tiebreak selection | As a playoff scorekeeper, I must select a winner if hole points tie. | Non-pod matches cannot publish tied; UI prompts winner selection when scorecard winningTeamId is null; API verifies winner is one of the teams. | `src/components/private-match-workspace.tsx`, `app/api/matches/[token]/scorecard/route.ts`, `src/lib/scoring/engine.ts` | Unit scoring tests; browser/API tied playoff publish with and without winner. | pass (automated), blocked manual | Browser/API publish mutates match data and requires safe DB state. | `npm test` covers scoring and strict route validation; manual tied-playoff publish blocked by invalid DB credentials. |
| Published read-only card | As a player, I cannot alter a final card from a normal private link. | Published final/submitted/forfeit status renders read-only public scorecard view and POST mutations are rejected without valid admin override. | `app/match/[token]/scorecard/page.tsx`, `src/components/private-match-workspace.tsx`, `app/api/matches/[token]/scorecard/route.ts` | Unit private routing; browser try to edit published card; API POST without admin override. | pass (automated), blocked manual | Browser read-only card requires DB-backed final match. | Route guard covered by tests; manual render blocked by invalid DB credentials. |
| Admin authentication | As commissioner, I can sign in with a configured 4-digit passcode and get a protected admin session. | If env missing, admin page shows configuration warning; login validates 4 digits, compares password, creates signed session cookie, and logout clears it. | `app/admin/page.tsx`, `app/admin/actions.ts`, `src/lib/server/admin-auth.ts` | Browser login page; login valid/invalid; inspect unauthenticated redirects. | fixed, blocked auth submit | Configured login card initially had no visible context text, only placeholder/button; full auth/dashboard submit is blocked by invalid configured DB credentials in this environment. | Added visible commissioner-desk header/logo to login card and retested `/admin` DOM/console. Full auth remains blocked until valid DB credentials/session are available. |
| Admin activity overview | As commissioner, I can see live/final/needs counts, production check, score rescue, and recent activity. | Authenticated `/admin` aggregates latest tournament data, counts match statuses, filters dry-run audit logs, supports load more. | `app/admin/page.tsx`, `src/lib/server/admin.ts` | Browser load admin overview after auth. | blocked | Requires admin passcode/session. |  |
| Admin email manager | As commissioner, I can generate private match links and send/copy invite messages. | Section generates pod match links once, builds Gmail compose URLs for players with emails, tracks sent state in localStorage, and flags missing emails/no matchup. | `app/admin/page.tsx`, `src/components/admin-match-ops-list.tsx`, `src/components/email-invite-button.tsx`, `app/admin/actions.ts` | Browser generate links, filter rows, copy/email invite. | blocked | Requires admin passcode/session; Gmail compose is external user action. |  |
| Admin scorecard manager | As commissioner, I can search, filter, open, continue, reset, and edit cards. | Section filters by setup/empty/live/final/waiting field; links to admin setup/scorecard routes; reset asks for browser confirmation and calls server action. | `app/admin/page.tsx`, `src/components/admin-match-ops-list.tsx`, `app/admin/match/[token]/*`, `app/admin/actions.ts` | Browser filter/search/open/reset after auth. | blocked | Requires admin passcode/session. |  |
| Admin final-card override | As commissioner, I can correct a posted card. | Admin routes require auth; final cards open in admin override mode; drafts set `REOPENED`; publishing writes audit log and new official snapshot. | `app/admin/match/[token]/scorecard/page.tsx`, `src/components/private-match-workspace.tsx`, `app/api/matches/[token]/scorecard/route.ts` | Unit private routing; browser/admin API override. | pass (automated), blocked manual | Browser/admin API override requires auth and safe DB state. | `npm test` covers admin route guard; manual blocked by invalid DB credentials. |
| Admin reopen/reset/forfeit | As commissioner, I can reopen, reset, or mark forfeit with audit trail. | Server actions require admin session, update match status, clear snapshots/scores when needed, write `MatchAuditLog`, and sync bracket. | `app/admin/actions.ts`, `src/lib/server/bracket-sync.ts`, `src/lib/server/official-result-snapshot.ts` | Unit bracket/snapshot tests; action-level manual/API test after auth. | pass (automated), blocked manual | Actions mutate DB and require admin session plus safe DB state. | Automated bracket/snapshot coverage passed; manual blocked by invalid DB credentials. |
| Admin roster/team/pod/import workflows | As commissioner, I can create/update players, teams, pods, pod assignments, and bulk import roster CSV. | Zod/form parsers enforce required fields, unique players per team, pod slot limits, duplicate-team protections, and existing-player roster checks. | `app/admin/actions.ts`, `src/lib/server/admin-validation.ts`, `src/lib/server/admin-import.ts` | Unit validation/import tests; manual form submission if UI exists. | pass (automated), blocked manual | No visible UI for several create/update actions in current admin page; actions mutate DB and require admin session plus safe DB state. | Validation/import unit tests passed; manual blocked by invalid DB credentials. |
| Admin bracket settings and sync | As commissioner, I can create/update bracket shell and sync advancement. | Bracket action creates standard QF/SF/Champ rounds if missing; sync computes seeds, creates shell matches, resets stale slots, and advances winners. | `app/admin/actions.ts`, `src/lib/server/bracket-sync.ts` | Unit bracket-sync/tournament progression tests; manual sync after auth. | pass (automated), blocked manual | Sync mutates DB and requires admin session plus safe DB state. | Bracket-sync and tournament-progression tests passed; manual blocked by invalid DB credentials. |
| Admin graphics UI | As commissioner, I can preview/download Instagram recap graphics for final matches. | Graphics section lists final/forfeit recaps, provides preview/download links, and client canvas generator can render PNG. | `app/admin/page.tsx`, `src/components/admin-instagram-graphic-generator.tsx`, `src/lib/server/admin-graphics.ts` | Unit export tests; browser preview/download after auth. | pass (automated), blocked manual | Browser graphics section requires admin session and DB-backed final matches. | `npm test` passed graphic export tests; manual blocked by invalid DB credentials. |
| Admin graphics API | As commissioner, I can download/open a graphic PNG by match id. | Auth required when admin auth is configured; resolves recap, renders SVG with embedded logo/font, converts via resvg, returns PNG inline/download. | `app/api/admin/graphics/[id]/route.ts` | Unit API export tests; browser hit route authenticated/unauthenticated. | pass (automated), blocked manual | Authenticated browser/API path requires admin session and DB-backed recaps. | `npm test` passed graphic export tests; manual blocked by invalid DB credentials. |
| Admin bootstrap API | As an authenticated preview bootstrapper, I can retrieve dashboard summary. | POST requires admin auth; returns 401 if unauthenticated, 503 if DB/tournament missing, otherwise tournament and summary. | `app/api/admin/tournament/bootstrap/route.ts` | Unit API tests. | pass | None yet. | `npm test` passed bootstrap route. |
| Sentry verification API | As commissioner, I can trigger a controlled Sentry error after deploy. | GET requires admin auth, captures controlled exception, flushes Sentry, returns event id. | `app/api/admin/sentry-test/route.ts`, `instrumentation.ts`, `sentry.*.config.ts` | Browser/API after auth; verify no unauth access. | blocked | Requires admin session and external Sentry verification. |  |
| Public standings API | As an integration client, I can fetch standings JSON. | GET `/api/public/tournament/[slug]/standings` returns pod standings, wild cards, projections, bubble or 404. | `app/api/public/tournament/[slug]/standings/route.ts` | Unit API test; curl/browser valid/invalid slug. | pass (automated), blocked manual | Runtime API requires working DB credentials. | `npm test` passed public standings API; manual curl with real data blocked by invalid DB credentials. |
| Public bracket API | As an integration client, I can fetch bracket JSON. | GET `/api/public/tournament/[slug]/bracket` returns bracket, seeds, rounds or 404. | `app/api/public/tournament/[slug]/bracket/route.ts` | Browser/API valid/invalid slug. | blocked | Runtime API requires working DB credentials. | Manual curl with real data blocked by invalid DB credentials; build/typecheck passed route. |
| Private scorecard API read | As the client app, I can hydrate a private scorecard by token. | GET returns `PrivateMatchView` for valid token or 404. | `app/api/matches/[token]/scorecard/route.ts`, `src/lib/server/matches.ts` | Browser/API valid/invalid token. | blocked | Runtime API requires DB-backed private match records. | Manual GET blocked by invalid DB credentials; private route behavior covered by routing tests. |
| Scoring engine | As the app, I can compute course handicap, playing handicap, strokes, points, ties, wins, forfeits, and snapshots deterministically. | Pure TypeScript engine validates setup, applies 90% allowance and max one stroke per hole, scores net better-ball and result codes. | `src/lib/scoring/engine.ts`, `src/lib/scoring/types.ts` | Unit scoring tests. | pass | None yet. | `npm test` passed scoring engine. |
| Official snapshots | As the app, final results remain stable after later data changes. | Final/forfeit matches store `officialResultSnapshot` with version and generated timestamp; public/admin reads prefer snapshot for official scorecards. | `src/lib/server/official-result-snapshot.ts`, scorecard route/actions | Unit snapshot tests; publish/reopen retest. | pass (automated), blocked manual | Publish/reopen runtime tests mutate DB and require safe DB state. | Unit snapshot tests passed; manual blocked by invalid DB credentials. |
| Public scorecard corrections | As an operator, known legacy scorecard corrections can be applied without changing core scoring. | Targeted correction module adjusts named match scorecards and recomputes affected summaries. | `src/lib/server/public-scorecard-corrections.ts`, `tests/public-scorecard-corrections.test.ts` | Unit correction tests; TypeScript check. | fixed | Test fixture empty `holeScores` arrays inferred as `never[]`, causing `npx tsc --noEmit` failures even though Vitest passed. | Typed the empty correction fixtures as `TestHoleScore[]`; `npx tsc --noEmit` and `npm test` now pass. |
| Course catalog providers | As a scorekeeper, course lookup uses stored catalog, curated fallbacks, GolfCourseAPI, OpenGolfAPI, and USGA fallback where configured. | Search dedupes/merges results, hydrates tee holes from siblings, persists up to 12 lookup results, and serializes tee/course data. | `src/lib/server/course-catalog.ts`, `src/lib/providers/*`, `data/course-catalog/*` | Unit fallback tests; browser/API course lookup; external provider blocked without keys/network. | pass (automated), blocked manual | Runtime search requires DB/provider credentials; external providers may be blocked in local audit. | Course catalog fallback tests passed; manual runtime blocked by invalid DB credentials. |
| GHIN integration boundary | As a future operator, handicap sync is represented but not active. | Player model stores GHIN and sync status; placeholder GHIN provider throws; manual Handicap Index remains active workflow. | `prisma/schema.prisma`, `src/lib/providers/types.ts`, `src/lib/providers/usga-scrape-provider.ts` | Code inspection; no live sync expected. | pass (code inspection) | GHIN sync is intentionally not operational. | No fix needed. |
| Database model integrity | As the app, tournament/player/team/pod/match/course/score/audit/feed/invite data persists with uniqueness and cascade rules. | Prisma schema defines enums, unique slugs/tokens, team roster/pod slot uniqueness, course tee/hole uniqueness, score uniqueness, cache/sync logs. | `prisma/schema.prisma` | Code inspection; build/typecheck; seed smoke. | pass (static), blocked runtime | Runtime DB smoke blocked by invalid configured Neon credentials and no local Postgres. | Prisma schema inspected; `npx tsc --noEmit` and `npm run build` pass. |
| Seed data | As a local developer, I can seed demo tournament, teams, pods, courses, bracket, match, invitations, feed, and sync logs. | `prisma/seed.ts` deletes existing data and populates demo state from mock data. | `prisma/seed.ts`, `src/lib/demo/mock-data.ts` | Optional `npm run prisma:seed` against local DB. | blocked | Destructive local DB reset; only run if needed/approved for local manual QA. |  |
| Operator scripts | As an operator, I can import field data, import/export courses, stage bracket preview, reset tournament state, backfill snapshots, generate rollout assets, and enforce synced deploy. | Scripts wrap CSV imports, provider ingestion, dry runs, reset/stage actions, snapshot backfill, and deployment preflight. | `scripts/*`, `package.json` scripts | Unit tests where present; dry-run selected scripts only when DB/network safety is clear. | blocked | Several scripts can mutate DB or require network/API credentials. | Not run because configured DB credentials fail and no safe local DB exists; package scripts and script inventory inspected. |
| Deployment/config | As a deployer, build and runtime config are sane. | Next config allows Sentry tunnel/source maps behavior, Vercel config points install/build output, deploy script refuses unsynced GitHub state. | `next.config.ts`, `vercel.json`, `scripts/require_github_synced_before_deploy.mjs`, `AGENTS.md` | `npm run lint`, `npx tsc --noEmit`, `npm run build`; no production deploy. | pass | None yet. | Lint, TypeScript, and production build passed. Build route table includes public, admin, private match, and API routes. |
| Error/loading/metadata surfaces | As a user, I get branded loading, not-found, and global error states. | App defines layout metadata, robots/sitemap, loading shells, branded not-found, global error, and match loading shells. | `app/layout.tsx`, `app/loading.tsx`, `app/not-found.tsx`, `app/global-error.tsx`, route loading files, `app/robots.ts`, `app/sitemap.ts` | Browser invalid route; build metadata routes; curl static metadata. | pass | None yet. | Browser checked `/not-a-real-route`: branded not-found copy, return-home link, no overlay text, no console errors. `robots.txt` and `sitemap.xml` returned 200; build passed. |
| Observability/privacy | As operator, telemetry captures useful errors without leaking private match tokens. | Sentry init is gated by DSN; scrubber redacts match URLs/tokens; Clarity script only renders with env id; Vercel Speed Insights included. | `instrumentation*.ts`, `src/lib/sentry-scrub.ts`, `src/components/clarity-script.tsx`, `app/layout.tsx` | Unit/code inspection; build. | pass (static), blocked external verification | Live Sentry verification requires admin session and external Sentry project access. | Build passed with instrumentation; Sentry test route manual verification blocked by invalid DB/admin session and external Sentry access. |
